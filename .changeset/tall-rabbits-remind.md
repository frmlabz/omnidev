---
"@omnidev-ai/adapters": patch
"@omnidev-ai/cli": patch
"@omnidev-ai/core": patch
---

Add a generated-file warning to `AGENTS.md` and `CLAUDE.md`, remind users to update `.gitignore` when enabling a provider, and preserve full skill directories when syncing provider skill outputs so reference files are copied alongside `SKILL.md`.
