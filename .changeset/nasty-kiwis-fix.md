---
"@omnidev-ai/core": minor
"@omnidev-ai/cli": minor
---

Add capability-local skill interpolation for static and programmatic skills using `{OMNIDEV_*}` placeholders backed by `.env` values, with shell environment variables taking precedence. Document the new skill templating behavior alongside the existing MCP interpolation flow.
