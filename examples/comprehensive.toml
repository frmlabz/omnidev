# Comprehensive Configuration
# Mixed sources demonstrating all OmniDev features

[capabilities.sources]
# Active fixtures from this repository
standard = { source = "github:Nikola-Milovic/omnidev", path = "examples/fixtures/standard" }
claude-plugin = { source = "github:Nikola-Milovic/omnidev", path = "examples/fixtures/claude-plugin" }
bare-skills = { source = "github:Nikola-Milovic/omnidev", path = "examples/fixtures/bare-skills" }

# Example: External GitHub sources (commented - replace with real repos)
# tasks = { source = "github:your-org/tasks-capability", version = "v2.1.0" }
# ralph = { source = "github:your-org/ralph-orchestrator", version = "v1.0.0" }

# Example: Local development sources (commented - requires local dirs)
# ui-components = "file://./capabilities/ui-components"
# my-tool = "file:///home/user/dev/my-capability"

# Example: Community capabilities (commented)
# obsidian = "github:kepano/obsidian-skills"

# Profiles for different workflows
[profiles.default]
capabilities = ["standard"]

[profiles.extended]
# Extended profile with multiple capabilities
capabilities = ["standard", "claude-plugin", "bare-skills"]

# Example profiles (commented - require external capabilities)
# [profiles.planning]
# capabilities = ["standard", "ralph"]

# [profiles.full-stack]
# capabilities = ["standard", "ralph", "ui-components", "my-tool"]
