# Basic OmniDev Config
# Minimal setup with a single capability

[capabilities.sources]
standard = { source = "github:frmlabz/omnidev", path = "examples/fixtures/standard" }

[profiles.default]
capabilities = ["standard"]
